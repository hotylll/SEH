# 信息收集整合系统

## 答辩演示检查清单

| 字段 | 内容 |
|------|------|
| 文档编号 | DEMO-CHECK-001 |
| 执行标准 | 参考 GB8567——88 文档格式 |
| 版本 | V1.1 |
| 密级 | 内部 |
| 编写日期 | 2026-06-24 |
| 编写人 | 罗元恒 |
| 用途 | 答辩前演示流程检查、截图准备、最终归档 |

---

## 1 演示前准备

| 序号 | 检查项 | 命令或方法 | 预期结果 |
|:----:|--------|------------|----------|
| 1 | 清理旧服务 | 确认 8000 端口未被占用 | 服务可正常启动 |
| 2 | 安装依赖 | `python3 -m pip install -r requirements.txt` | openpyxl/reportlab 可用 |
| 3 | 启动后端 | `python3 -m app.main --host 127.0.0.1 --port 8000` | 终端显示服务启动 |
| 4 | 打开前端 | 浏览器打开 `frontend/index.html` | 首页可访问 |
| 5 | 执行测试 | `python3 -m unittest discover -s tests -v` | 21 个测试全部通过 |
| 6 | 检查数据 | 前端点击“刷新” | 首页、趋势、信息列表有数据 |

---

## 2 推荐演示流程

### 2.1 系统首页

1. 打开 `frontend/index.html`。
2. 确认服务状态显示 `ok`。
3. 展示热点主题数、信息总数、数据源数。
4. 展示今日热点榜和热度分布。

截图建议：系统首页全屏截图。

### 2.2 数据源管理

1. 切换到“数据源管理”。
2. 新增一个数据源：

```text
名称：答辩演示源
类型：news
endpoint：https://example.com/demo
keywords：人工智能,新能源汽车
schedule：weekly
status：enabled
```

3. 点击“采集”按钮。
4. 确认提示中出现任务状态和新增条数。

截图建议：数据源列表 + 采集结果提示。

### 2.3 信息搜索

1. 切换到“信息搜索”。
2. 输入关键词 `人工智能`。
3. 点击搜索。
4. 展示标题、来源、发布时间、关键词、质量评分。
5. 点击“详情”，展示清洗正文和质量评分。

截图建议：搜索结果表格。

### 2.4 趋势分析

1. 切换到“趋势分析”。
2. 点击刷新趋势。
3. 展示主题、分数、趋势方向和统计周期。
4. 点击主题“详情”，展示关联信息列表。
5. 说明趋势方向支持 `surge/up/stable/down/plunge` 五类。

截图建议：趋势榜单。

### 2.5 报表中心

1. 切换到“报表中心”。
2. 选择 `summary`。
3. 格式选择 `xlsx` 或 `pdf`。
4. 生成人填写 `罗元恒`。
5. 点击生成报表。
6. 展示报表名称、文件路径和下载按钮，并下载文件。

截图建议：报表生成结果。

---

## 3 必备截图清单

| 编号 | 截图内容 | 用途 |
|:----:|----------|------|
| 图1 | 后端服务启动终端 | 证明系统可运行 |
| 图2 | 单元/集成测试全部通过 | 证明质量控制 |
| 图3 | 前端首页 Dashboard | 展示总体效果 |
| 图4 | 数据源管理与采集任务 | 展示采集能力 |
| 图5 | 信息搜索结果 | 展示检索能力 |
| 图6 | 趋势分析榜单 | 展示分析能力 |
| 图7 | 报表生成和下载结果 | 展示 XLSX/PDF 报表能力 |
| 图8 | 系统架构图 | 说明设计方案 |

---

## 4 答辩讲解要点

1. 罗元恒负责整体架构、核心代码、接口联调、测试验收和最终归档。
2. 系统采用 Python 标准库为主，结合 openpyxl/reportlab 完成 XLSX/PDF 报表导出，部署复杂度仍保持可控。
3. 数据流为：数据源配置 → 采集任务 → 原始数据入库 → 清洗评分 → 趋势分析 → 前端展示。
4. SQLite 表结构覆盖用户、数据源、任务、原始数据、清洗数据、趋势、报表、审计日志。
5. 测试覆盖单元测试、接口集成测试和 10000 条数据性能测试。
6. 后续扩展方向包括真实爬虫接入、登录权限、定时调度和图表库增强。

---

## 5 演示风险与处理

| 风险 | 处理方式 |
|------|----------|
| 8000 端口占用 | 改用 `--port 8001`，并在前端 API 输入框修改地址 |
| 页面无数据 | 点击数据源管理中的“采集”，再刷新首页 |
| 浏览器跨域错误 | 确认后端已启动并使用当前版本 `app/api.py` |
| 重复采集新增条数为 0 | 属于去重机制正常现象，可新增不同关键词数据源重新采集 |
| 报表生成失败 | 先执行 `python3 -m pip install -r requirements.txt`，确认 report_type 和 format 参数正确 |

---

**编写人：罗元恒**
**审核人：罗元恒**
