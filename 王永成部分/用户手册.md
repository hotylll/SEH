# 信息收集整合系统

## 用户手册

---

| 字段 | 内容 |
|------|------|
| **文档编号** | UM-001 |
| **执行标准** | GB8567——88 |
| **版本** | V1.1 |
| **密级** | 内部 |
| **原编写人** | 王永成 |
| **修订人** | 罗元恒 |
| **修订日期** | 2026-06-24 |

---

## 版本记录

| 版本 | 日期 | 修改内容 | 修改人 |
|:----:|:----:|---------|:------:|
| V1.0 | 2026-06-23 | 形成用户手册初稿 | 王永成 |
| V1.1 | 2026-06-24 | 补充详情查看、主题详情、XLSX/PDF 报表下载和当前版本边界说明 | 罗元恒 |

---

## 1 引言

### 1.1 编写目的

本手册面向**普通浏览用户**和**信息分析员**，说明如何浏览热点趋势、检索信息明细、查看详情以及导出分析报表。当前版本以课程演示为主，不包含登录认证和权限拦截；登录权限作为后续扩展说明。

### 1.2 适用对象

| 用户角色 | 主要用途 | 本手册相关章节 |
|----------|----------|----------------|
| 普通浏览用户 | 查看热点榜、搜索信息、阅读详情 | 第 3、4、5 章 |
| 信息分析员 | 趋势研判、主题对比、导出报表 | 第 4、5、6 章 |

系统管理员的操作（数据源配置、采集任务、审计日志等）请参阅《操作手册》。

### 1.3 术语说明

| 术语 | 说明 |
|------|------|
| 热点榜 | 按热度分数排序的主题列表 |
| 趋势方向 | up（上升）、down（下降）、stable（稳定）、surge（突增） |
| 质量评分 | 清洗模块对单条信息完整度的 0~100 分评价 |
| 信息明细 | 经采集和清洗后的结构化信息条目 |

### 1.4 运行环境

**表1-1 客户端环境要求**

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome 90+、Edge 90+、Firefox 88+ |
| 屏幕分辨率 | 建议 1280×720 及以上 |
| 网络 | 能访问系统服务器（默认 `http://127.0.0.1:8000`） |

---

## 2 系统访问

### 2.1 启动与访问

1. 确认后端服务已启动（由管理员或开发人员执行）：

```powershell
python -m app.main --host 127.0.0.1 --port 8000
```

2. 在浏览器地址栏输入系统地址，进入**系统首页（Dashboard）**。
3. 首次访问时，系统自动加载演示数据，首页应显示热点主题和最新信息摘要。

### 2.2 页面导航

**表2-1 主导航菜单**

| 菜单项 | 页面 | 功能 |
|--------|------|------|
| 首页 | Dashboard | 今日热点、趋势概览、来源分布 |
| 趋势分析 | /trends | 主题榜单、趋势方向、主题详情 |
| 信息搜索 | /items | 关键词检索、筛选、详情查看 |
| 报表中心 | /reports | 导出分析报表（分析员） |

### 2.3 健康检查

若页面无法加载数据，可在浏览器访问：

```text
http://127.0.0.1:8000/api/v1/health
```

正常时应返回 `"status": "ok"`。若返回错误，请联系系统管理员。

---

## 3 首页使用说明

### 3.1 页面布局

首页分为四个区域：

1. **顶部导航栏**：系统名称、主导航和 API 地址配置。
2. **今日热点榜**：展示热度排名前 10 的主题及分数、趋势方向。
3. **热度分布**：以横向条形图展示前 8 个热点主题。
4. **统计卡片**：展示服务状态、热点主题数、信息总数和数据源数。

### 3.2 查看今日热点

**操作步骤：**

1. 进入首页，默认展示「今日热点榜」。
2. 每条热点显示：**主题名称**、**热度分数**、**趋势方向图标**。
3. 点击主题行右侧「详情」按钮，可打开主题详情弹窗。
4. 点击「刷新」按钮重新加载最新数据。

**表3-1 趋势方向图标含义**

| 方向 | 图标 | 含义 |
|------|------|------|
| up | ↑ | 热度较上一周期上升超过 20% |
| down | ↓ | 热度下降超过 20% |
| stable | → | 热度变化在 ±20% 以内 |
| surge | ⚡ | 新出现或突增主题 |

### 3.3 查看统计概览

1. 首页统计卡展示服务状态、热点主题数量、信息总数和数据源数量。
2. 热度分布条形图用于快速判断主题热度排序。
3. 如需查看具体信息内容，可进入「信息搜索」页面点击「详情」。

---

## 4 趋势分析

### 4.1 进入趋势分析页

- 方式一：点击顶部导航「趋势分析」。
- 方式二：在首页热点榜中点击某一主题。

### 4.2 查看趋势榜单

**表4-1 趋势榜单字段说明**

| 列名 | 说明 |
|------|------|
| 排名 | 按 score 降序排列 |
| 主题 | 关键词或主题名称 |
| 热度分数 | 综合频次与时间衰减的计算结果 |
| 趋势方向 | up / down / stable / surge |
| 统计周期 | period_start ~ period_end |

**操作步骤：**

1. 打开趋势分析页，默认展示前 20 个主题。
2. 使用「显示条数」下拉框选择 10 / 20 / 50。
3. 点击某一主题行的「详情」，展开该主题的热度分数、趋势方向、统计周期和关联信息列表。

### 4.3 查看单主题详情

点击「详情」后，系统以弹窗展示：

- 主题名称与当前热度分数
- 趋势方向及简要解读
- 统计周期（起止日期）
- 相关信息列表（标题、来源、发布时间链接）

**对应 API（供联调参考）：**

```text
GET /api/v1/trends?limit=20
GET /api/v1/trends/{topic}
```

**响应示例：**

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "topics": [
      {
        "topic": "人工智能",
        "score": 24.0,
        "direction": "up",
        "period_start": "2026-06-15",
        "period_end": "2026-06-18"
      }
    ]
  }
}
```

### 4.4 趋势研判建议

分析员在使用趋势榜时，建议关注：

1. **surge** 方向主题：可能是新兴热点，需结合信息明细核实。
2. **score 高且 direction 为 up**：持续升温主题，适合纳入报告。
3. **direction 为 down 但 score 仍高**：存量热点降温，可对比历史周期。

---

## 5 信息搜索

### 5.1 进入搜索页

点击顶部导航「信息搜索」，进入信息检索页面。

### 5.2 基本搜索

**操作步骤：**

1. 在搜索框输入关键词，如 `人工智能`、`新能源汽车`。
2. 点击「搜索」或按 Enter 键。
3. 结果列表展示匹配的信息条目。

**表5-1 搜索结果字段**

| 字段 | 说明 |
|------|------|
| 标题 | normalized_title 或原始 title |
| 来源 | source_id 对应的数据源编号 |
| 发布时间 | published_at，ISO8601 格式 |
| 关键词 | 清洗模块提取的关键词，逗号分隔 |
| 质量评分 | 0~100，越高表示内容越完整 |

### 5.3 高级筛选

在搜索工具栏中可组合以下条件：

| 筛选项 | 说明 |
|--------|------|
| 关键词 | 标题、正文、关键词模糊匹配 |
| 来源 | 输入 source_id 进行过滤 |
| 显示条数 | 默认 50，最大 100 |

**对应 API：**

```text
GET /api/v1/items?keyword=人工智能&limit=50
GET /api/v1/items?keyword=人工智能&source_id=1&limit=20
```

### 5.4 查看信息详情

1. 在搜索结果中点击某条信息的「详情」按钮。
2. 详情弹窗展示：

**表5-2 详情页内容区块**

| 区块 | 内容 |
|------|------|
| 基本信息 | 标题、来源、作者、发布时间、原文链接 |
| 正文 | 清洗后的 normalized_content |
| 关键词 | 系统提取的关键词标签 |
| 质量评分 | 数值及简要说明（如「正文充实」「链接有效」） |
| 关联主题 | 与该信息相关的热点主题（如有） |

**对应 API：**

```text
GET /api/v1/items/{item_id}
```

3. 点击「关闭」回到搜索结果页，保留上次筛选条件。

---

## 6 报表导出

### 6.1 适用角色

报表导出功能主要面向**信息分析员**。当前演示版本未启用登录权限拦截，后续可在登录模块完成后按角色限制导出入口。

### 6.2 导出入口

1. 进入「报表中心」页面。
2. 选择报表类型和导出格式。
3. 点击「生成报表」后，在报表记录列表中下载文件。

### 6.3 导出步骤

1. 选择报表类型：
   - **摘要报表（summary）**：热点主题 + 统计周期概览
   - **明细报表（detail）**：信息条目列表
2. 选择导出格式：`xlsx` 或 `pdf`。
3. 填写「生成人」标识（默认罗元恒）。
4. 点击「生成报表」，系统生成真实文件并返回报表记录。

**对应 API：**

```text
POST /api/v1/reports
Content-Type: application/json

{
  "report_type": "detail",
  "format": "xlsx",
  "generated_by": "罗元恒"
}
```

**成功响应：**

```json
{
  "code": 0,
  "data": {
    "id": 1,
    "report_name": "trend-report-20260624_160000-detail.xlsx",
    "report_type": "detail",
    "file_format": "xlsx",
    "file_path": "data/reports/trend-report-20260624_160000-detail.xlsx",
    "generated_by": "罗元恒",
    "generated_at": "2026-06-24T16:00:00+00:00",
    "download_url": "/api/v1/reports/1/download"
  }
}
```

5. 在报表中心查看历史导出记录，点击「下载」获取 XLSX 或 PDF 文件。

### 6.4 报表使用建议

- 答辩或汇报前，建议先导出一版 summary/xlsx 报表作为附件。
- 导出前确认趋势数据已更新（管理员需先完成采集任务）。
- 报表文件名含时间戳，便于版本区分。

---

## 7 常见问题

**表7-1 常见问题与处理**

| 问题 | 可能原因 | 处理办法 |
|------|----------|----------|
| 首页无热点数据 | 尚未执行采集任务 | 联系管理员启动采集 |
| 搜索无结果 | 关键词不匹配或数据未入库 | 更换关键词或放宽筛选 |
| 趋势榜为空 | topic_trends 表无记录 | 确认采集成功后刷新页面 |
| 详情页链接打不开 | 原文 URL 为演示地址 | 属正常现象，以系统内正文为准 |
| 报表生成失败 | 参数格式错误或服务异常 | 查看 health 接口，确认 report_type/format 是否正确 |
| 页面加载超过 3 秒 | 数据量过大或网络延迟 | 缩小 limit，检查服务器负载 |

---

## 8 快速操作索引

**表8-1 常用操作速查**

| 我想… | 操作路径 |
|-------|----------|
| 看今天什么最火 | 首页 → 今日热点榜 |
| 查某个主题的趋势 | 趋势分析 → 搜索/点击主题 |
| 搜某关键词的信息 | 信息搜索 → 输入关键词 → 搜索 |
| 看一条信息的全文 | 搜索结果 → 详情 |
| 导出分析报告 | 报表中心 → 选择类型 → 生成 |
| 确认系统是否正常 | 浏览器访问 /api/v1/health |

---

## 9 附录

### 9.1 界面与 API 对应关系

| 界面功能 | API 路径 |
|----------|----------|
| 健康状态 | GET /api/v1/health |
| 热点趋势榜 | GET /api/v1/trends |
| 主题详情 | GET /api/v1/trends/{topic} |
| 信息搜索 | GET /api/v1/items |
| 信息详情 | GET /api/v1/items/{id} |
| 生成报表 | POST /api/v1/reports |
| 报表列表 | GET /api/v1/reports |
| 报表下载 | GET /api/v1/reports/{id}/download |

### 9.2 相关文档

- 《操作手册》— 管理员配置与运维
- 《界面设计说明》— 页面布局与交互规范
- 《软件需求说明书》— 功能需求原文

---

**原编写人：王永成**
**修订人：罗元恒**
**审核人：罗元恒**
**批准日期：2026-06-24**
