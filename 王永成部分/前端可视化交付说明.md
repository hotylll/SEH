# 信息收集整合系统

## 前端可视化交付说明

| 字段 | 内容 |
|------|------|
| 文档编号 | FE-DELIVERY-001 |
| 版本 | V1.0 |
| 编写日期 | 2026-06-23 |
| 模块负责人 | 王永成 |
| 审查人 | 组长 |
| 对应分工 | 仪表盘、趋势图、数据表格等前端编码；UI/UX 设计文档 |

---

## 1 交付目标

本交付说明用于记录前端可视化模块的实现内容。前端页面用于课程答辩演示，覆盖系统首页、趋势分析、信息搜索、数据源管理和报表中心，能够通过 REST API 调用后端服务，展示信息收集整合系统的主要业务闭环。

---

## 2 交付文件

| 文件 | 说明 |
|------|------|
| `frontend/index.html` | 前端演示页面，包含 HTML、CSS、JavaScript |
| `王永成部分/界面设计说明.md` | 页面结构、视觉规范、线框图、交互说明 |
| `王永成部分/用户手册.md` | 普通用户和分析员使用说明 |
| `王永成部分/操作手册.md` | 管理员安装、配置、采集和维护说明 |

---

## 3 页面功能

| 页面 | 功能 | 对应 API |
|------|------|----------|
| 系统首页 | 服务状态、热点主题数、信息总数、数据源数、热点榜、热度分布 | `/api/v1/health`, `/api/v1/trends`, `/api/v1/items`, `/api/v1/sources` |
| 趋势分析 | 展示主题、分数、趋势方向、统计周期 | `/api/v1/trends` |
| 信息搜索 | 按关键词、来源 ID、数量限制查询信息明细 | `/api/v1/items` |
| 数据源管理 | 新增数据源、查看数据源列表、触发采集任务 | `/api/v1/sources`, `/api/v1/tasks/collect` |
| 报表中心 | 生成 summary/detail 报表记录 | `/api/v1/reports` |

---

## 4 运行方式

1. 启动后端服务：

```bash
python3 -m app.main --host 127.0.0.1 --port 8000
```

2. 使用浏览器打开：

```text
frontend/index.html
```

3. 页面默认连接：

```text
http://127.0.0.1:8000
```

若后端端口变化，可在页面右上角 API 输入框修改地址并保存。

---

## 5 联调结果

| 测试项 | 结果 |
|--------|:----:|
| 页面可打开 | 通过 |
| 健康检查可读取 | 通过 |
| 热点榜可展示 | 通过 |
| 信息列表可检索 | 通过 |
| 数据源可创建 | 通过 |
| 采集任务可触发 | 通过 |
| 报表记录可生成 | 通过 |
| CORS 预检 | 通过 |

---

## 6 审查结论

前端可视化模块满足课程答辩演示需求，能够展示系统的核心业务链路。当前版本以静态页面实现，未引入第三方前端框架，部署简单，适合本课程项目规模。后续如继续扩展，可接入 Vue/React、图表库、登录权限和真实报表下载功能。

**模块负责人：王永成**  
**审查人：组长**
