# 信息收集整合系统

## 测试执行记录

| 字段 | 内容 |
|------|------|
| 文档编号 | TEST-RUN-001 |
| 执行标准 | 参考 GB8567——88 文档格式 |
| 版本 | V1.1 |
| 密级 | 内部 |
| 执行日期 | 2026-06-23 |
| 执行人 | 郭子凌 |
| 修订人 | 组长 |
| 修订日期 | 2026-06-24 |
| 审查人 | 组长 |
| 测试范围 | 单元测试、接口集成测试、性能冒烟测试 |

---

## 1 测试目的

本记录用于说明信息收集整合系统在当前版本下的实际测试执行情况，覆盖数据源配置、采集任务、数据清洗、趋势分析、信息检索、报表接口和 10000 条演示数据检索性能。测试结果由组长统一审查后纳入最终归档材料。

---

## 2 测试环境

| 项目 | 内容 |
|------|------|
| 操作系统 | Windows / WSL Linux |
| Python | 3.12 |
| 数据库 | SQLite |
| 运行方式 | `python3 -m unittest discover -s tests -v` |
| 依赖 | Python 标准库、openpyxl、reportlab |

---

## 3 测试用例清单

| 测试文件 | 用例数量 | 覆盖范围 |
|----------|:--------:|----------|
| `tests/test_analysis.py` | 8 | HTML 清洗、关键词提取、质量评分、趋势方向、时间衰减 |
| `tests/test_api.py` | 7 | 健康检查、HTTP 400 校验、CORS 预检、采集到检索完整接口流程、主题详情、报表下载 |
| `tests/test_storage.py` | 5 | 采集入库、趋势生成、去重、禁用数据源、weekly 调度 |
| `tests/test_performance.py` | 1 | 10000 条演示数据检索性能 |

总计：21 个测试用例。

---

## 4 关键测试结果

| 编号 | 测试项 | 预期结果 | 实际结果 | 结论 |
|:----:|--------|----------|----------|:----:|
| T-001 | 健康检查接口 | 返回 `status=ok` | 通过 | 通过 |
| T-002 | 非法数据源类型 | 返回 HTTP 400 | 通过 | 通过 |
| T-003 | weekly 调度配置 | 可创建数据源 | 通过 | 通过 |
| T-004 | 禁用数据源采集 | 拒绝采集 | 通过 | 通过 |
| T-005 | 采集任务 | 写入 raw_items 与 clean_items | 通过 | 通过 |
| T-006 | 重复采集 | 不重复入库，记录去重信息 | 通过 | 通过 |
| T-007 | 趋势分析 | 支持 surge/up/stable/down/plunge | 通过 | 通过 |
| T-008 | 时间衰减 | 旧数据热度低于新数据 | 通过 | 通过 |
| T-009 | API 集成流程 | 创建源、采集、检索、趋势均成功 | 通过 | 通过 |
| T-010 | 10000 条性能 | 检索响应小于 3 秒 | 通过 | 通过 |
| T-011 | XLSX/PDF 报表下载 | 生成文件并返回正确 Content-Type | 通过 | 通过 |
| T-012 | 非法请求参数 | 返回 HTTP 400 而不是 500 | 通过 | 通过 |

---

## 5 执行命令与结果

```bash
python3 -m unittest discover -s tests -v
```

当前执行结果：

```text
Ran 21 tests
OK
```

---

## 6 问题与处理

| 问题 | 处理方式 |
|------|----------|
| 接口层原缺少集成测试 | 已补充 `tests/test_api.py` |
| 原未验证 10000 条检索性能 | 已补充 `tests/test_performance.py` |
| 趋势方向与分析模块文档不一致 | 已在主线代码中支持 5 种趋势方向 |
| weekly 调度与文档不一致 | 已在 `DataSource` 校验中支持 `weekly` |
| 报表下载与异常参数未自动验证 | 已补充 XLSX/PDF 下载和错误输入测试 |

---

## 7 测试结论

当前版本测试通过，系统核心后端流程能够完成数据源配置、采集入库、清洗评分、趋势分析、信息检索、详情查看和 XLSX/PDF 报表下载。前端演示页可调用后端接口完成主要演示流程。建议在答辩前由组长执行一次完整演示检查，并保留测试通过截图作为答辩材料。

**执行人：郭子凌**  
**审查人：组长**
